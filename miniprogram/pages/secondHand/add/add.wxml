<!--miniprogram/pages/secondHand/add/add.wxml-->
<button bindtap="pickPhoto">上传图片</button>
<block wx:if="{{isUpload}}">
  <swiper indicator-dots="{{false}}" autoplay="{{false}}" style="height:500rpx">
    <block wx:for="{{fileID}}" wx:key="i">
      <swiper-item>
        <image src="{{item}}"></image>
      </swiper-item>
    </block>
  </swiper>
</block>
<view class="weui-cells weui-cells_after-title">
  <view class="weui-cell">
    <view class="weui-cell__bd">
      <textarea bindinput="infoInput" data-field="info" class="weui-textarea" placeholder="请输入文本" value="{{info}}"></textarea>
    </view>
  </view>
</view>
<picker bindchange="bindPickerChange" value="{{typeIndex}}" range="{{typeArray}}">
  <view class="picker">
    类型：{{typeArray[typeIndex]}}
  </view>
</picker>
<view class="weui-cells weui-cells_after-title">
  <view class="weui-cell">
    <view class="weui-cell__bd">
      <textarea bindinput="priceInput" data-field="price" class="weui-textarea"  placeholder="请输入价格" value="{{price}}"></textarea>
    </view>
  </view>
</view>
<button bindtap="submit">提交</button>
<button bindtap="next">清空</button>
<button bindtap="back">取消并返回</button>